import type { FC } from 'react';
import React from 'react';
import { Layout } from '../components/layout';

const Page: FC = () => (
  <Layout>
    <h1>Portfolio</h1>
    <p>作成中・・・</p>
  </Layout>
);

export default Page;
