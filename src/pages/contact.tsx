import type { FC } from 'react';
import React from 'react';
import { Layout } from '../components/layout';

const Page: FC = () => (
  <Layout>
    <h1>Contact</h1>
    <p>作成中・・・</p>
    {/* <a href={`mailto:${data.site?.siteMetadata?.email}`}>Mail</a> */}
  </Layout>
);
export default Page;
